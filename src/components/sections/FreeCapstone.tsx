export default function FreeCapstone() {
  return (
    <div className="capstone">
      <div className="free">
        FREE<span className="pd">.</span>
      </div>
      <p style={{ fontSize: 'clamp(20px,2.7vw,26px)', fontWeight: 800, color: '#FFFFFF', marginTop: '24px', lineHeight: 1.3 }}>
        Anonymity. Accountability.
      </p>
      <p style={{ fontSize: 'clamp(18px,2.4vw,23px)', fontWeight: 700, color: '#E2E8F0', marginTop: '8px', lineHeight: 1.4 }}>
        The most advanced protection available today.
      </p>
      <p style={{ fontSize: 'clamp(18px,2.4vw,23px)', fontWeight: 700, color: '#E2E8F0', marginTop: '14px', lineHeight: 1.4 }}>
        Onboard in minutes. Applied in seconds.
      </p>
      <p style={{ fontSize: 'clamp(18px,2.4vw,23px)', fontWeight: 700, color: '#E2E8F0', marginTop: '14px', lineHeight: 1.4 }}>
        Giving everyone peace of mind <span style={{ color: 'var(--vf)' }}>before</span>, <span style={{ color: 'var(--vf)' }}>during</span>, and <span style={{ color: 'var(--vf)' }}>after</span>.
      </p>
      <div style={{ height: '1px', background: 'rgba(139,92,246,.22)', maxWidth: '190px', margin: '30px auto' }}></div>
      <p style={{ fontSize: 'clamp(20px,2.8vw,28px)', fontWeight: 700, color: '#FFFFFF', lineHeight: 1.3 }}>
        The only reason to risk your freedom, reputation, or your life is cost.
      </p>
      <p style={{ fontSize: 'clamp(28px,5vw,46px)', fontWeight: 900, letterSpacing: '-.02em', marginTop: '8px', lineHeight: 1.1 }}>
        That's why we made it <span style={{ color: 'var(--vf)' }}>free</span>.
      </p>
      <div style={{ height: '1px', background: 'rgba(139,92,246,.22)', maxWidth: '190px', margin: '28px auto' }}></div>
      <p style={{ fontSize: 'clamp(22px,3.6vw,36px)', fontWeight: 800, letterSpacing: '-.01em', lineHeight: 1.15 }}>
        <img src="/brand/vairify-icon-dark-clear.svg" alt="Vairify" style={{ height: '40px', width: 'auto', verticalAlign: '-8px', marginRight: '6px' }} />
        <span style={{ color: 'var(--vf)' }}>The first standard.</span>
      </p>
    </div>
  );
}
